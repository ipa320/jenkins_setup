<?xml version='1.0' encoding='UTF-8'?>
<@(PROJECT)>
  <actions/>
  <description>AUTOMATICALLY GENERATED JOB, DO NOT EDIT BY HAND! This job was generated by @(USERNAME) on @(HOSTNAME) at @(TIME) from the python-jenkins-tools package, and runs the @(SCRIPT) script.</description>
  <logRotator>
    <daysToKeep>180</daysToKeep>
    <numToKeep>500</numToKeep>
    <artifactDaysToKeep>-1</artifactDaysToKeep>
    <artifactNumToKeep>-1</artifactNumToKeep>
  </logRotator>
  <keepDependencies>false</keepDependencies>
  @(MATRIX)
  <properties>
    @(PARAMETERIZED_JOB_PARAMETERS)
    @(AUTHORIZATIONMATRIX)
    <hudson.plugins.trac.TracProjectProperty>
        <tracWebsite>http://www.care-o-bot-research.org/trac</tracWebsite>
    </hudson.plugins.trac.TracProjectProperty>
  </properties>
  @(VCS)
  <assignedNode>@(NODE_LABEL)</assignedNode>
  @(QUIET_PERIOD)
  <canRoam>false</canRoam>
  <disabled>false</disabled>
  <blockBuildWhenUpstreamBuilding>@(BLOCKING_UPSTREAM)</blockBuildWhenUpstreamBuilding>
  <blockBuildWhenDownstreamBuilding>@(BLOCKING_DOWNSTREAM)</blockBuildWhenDownstreamBuilding>
  @(TRIGGER)
  <concurrentBuild>@(CONCURRENT_BUILD)</concurrentBuild>
  @(CUSTOM_WORKSPACE)
  @(SHELL)
  <publishers>
    @(POSTBUILD_TASK)
    @(GROOVY_POSTBUILD)
    @(PIPELINE_TRIGGER)
    @(JOIN_TRIGGER)
    @(POSTBUILD_TRIGGER)
    @(PARAMETERIZED_TRIGGER)
    @(JUNIT_TESTRESULTS)
    @(MAILER)
    @(WARNINGS_PUBLISHER)
    @(CPPCHECK_PUBLISHER)
  </publishers>
  <buildWrappers>
    @(BUILD_TIMEOUT)
  </buildWrappers>
</@(PROJECT)>
